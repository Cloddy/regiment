export const images: Record<string, string> = {
  triangle: require('./arrow.svg'),
  inputCross: require('./inputCross.svg'),
  trash: require('./trash.svg'),
};

export const statics: string[] = Object.values(images);
